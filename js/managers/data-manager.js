
class DataManager {
    constructor() {
        this.locationsData = null;
        this.regionsData = null;
    }

    async loadInitialLocations() {
        // Déplacer loadInitialLocations() ici
    }

    saveLocationsToLocal() {
        // Déplacer saveLocationsToLocal() ici
    }

    saveRegionsToLocal() {
        // Déplacer saveRegionsToLocal() ici
    }

    loadRegionsFromLocal() {
        // Déplacer loadRegionsFromLocal() ici
    }

    exportUnifiedData() {
        // Déplacer exportUnifiedData() ici
    }

    importUnifiedData(event) {
        // Déplacer importUnifiedData() ici
    }
}

export default DataManager;
