
class InfoBoxManager {
    constructor(domElements, dataManagers) {
        this.dom = domElements;
        this.dataManagers = dataManagers;
        this.activeLocationId = null;
    }

    showInfoBox(event) {
        // Déplacer showInfoBox() complète ici
    }

    hideInfoBox() {
        // Déplacer hideInfoBox() ici
    }

    enterEditMode() {
        // Déplacer enterEditMode() ici
    }

    saveEdit() {
        // Déplacer saveEdit() ici
    }

    cancelEdit() {
        // Déplacer cancelEdit() ici
    }

    showRegionInfo(event, region) {
        // Déplacer showRegionInfo() ici
    }

    // Toutes les autres fonctions liées aux info box
}

export default InfoBoxManager;
