Architecture actuelle :

Structure modulaire avec managers dans /js/managers/
main.js minimal avec imports et initialisation
Info-boxes fonctionnelles en lecture seule avec onglets (Image, Texte, Rumeurs, Tradition)
Managers existants :

DataManager : gestion des données (lieux/régions) avec sauvegarde
FilterManager, VoyageManager, PathManager, GeminiManager, etc.
Info-boxes gérées directement dans main.js avec fonctions showInfoBox(), hideInfoBox()
PROBLÈME ACTUEL : Les modales d'info-box affichent les informations mais le mode édition ne fonctionne pas.

OBJECTIF : Créer InfoBoxManager dans /js/managers/infobox-manager.js et implémenter le système d'édition complet.

Fonctionnalités à implémenter :

Système d'édition des lieux :

Bouton "éditer" dans l'info-box qui bascule en mode édition
Édition nom, description, images (URL), couleur, statuts (connu/visité)
Génération automatique de description via Gemini (bouton ✨)
Boutons Sauvegarder/Annuler
Système d'édition des régions :

Même interface d'édition adaptée aux régions
Champs : nom, description, rumeurs, tradition ancienne, couleur
Support des rumeurs multiples (séparées par saut de ligne)
Génération Gemini pour descriptions de régions
Gestion des images multiples :

Interface pour ajouter/supprimer des images par URL
Sélection de l'image par défaut
Aperçu des images dans l'onglet Image
Interface d'édition :

Transformation des onglets en mode édition (formulaires)
Sélecteurs de couleur interactifs
Validation des données avant sauvegarde
Gestion des erreurs
Structure souhaitée :

class InfoBoxManager {
    constructor(domUtils, dataManager, geminiManager)
    showInfoBox(event, item, type) // Affichage lecture
    hideInfoBox()
    enterEditMode() // Basculer en édition
    exitEditMode() // Sortir du mode édition
    saveEdit() // Sauvegarder les modifications
    generateDescription() // Appel Gemini
    setupEditListeners() // Événements d'édition
}
Code existant à adapter :

Fonctions showInfoBox(), hideInfoBox() dans main.js (à déplacer)
Logique d'édition présente dans attached_assets/main_1759168513821.js (références pour l'implémentation)
Interface HTML des onglets déjà présente dans index.html
Contraintes :

Garder main.js minimal
Utiliser les patterns existants pour la gestion des modals
Intégrer avec DataManager pour la sauvegarde
Utiliser GeminiManager pour la génération de contenu
Appeler scheduleAutoSync() après modifications pour synchronisation
Approche souhaitée :

Créer InfoBoxManager complet avec toutes les méthodes
Migrer les fonctions d'info-box depuis main.js vers le manager
Implémenter le système d'édition avec interface adaptée
Intégrer proprement dans main.js avec initialisation
Peux-tu créer le InfoBoxManager et adapter le code existant pour une gestion complète de l'édition des lieux et régions ?